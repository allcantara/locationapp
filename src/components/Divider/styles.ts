import styled from 'styled-components/native'
import stylesConfig from '../../config/stylesConfig'

export const DividerContainer = styled.View`
  width: 100%;
  height: 1px;
  background: ${stylesConfig.colorGray4};
`